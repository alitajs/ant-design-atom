/**
 * title: 基本
 * desc: 进度小图
 */

import React, { FC } from 'react';
import { MiniProgress } from 'ant-design-atom';

const Page: FC = () => <MiniProgress percent={78} target={90} />;

export default Page;
